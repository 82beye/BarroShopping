[project]
name = "shortsgen-backend"
version = "0.1.0"
description = "바로쇼핑 백엔드 — FastAPI 제어부 (FR-5)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
