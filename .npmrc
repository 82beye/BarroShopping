# @remotion/zod-types(peer zod@4)와 프로젝트의 zod@3 고정 간 peer 충돌 회피.
# 현재 zod@3 코드가 정상 렌더되므로 버전 변경 없이 설치만 통과시킨다.
# (정식 정렬: zod@4로 업그레이드 또는 zod-types 핀 — IMPLEMENTATION_PLAN P0 후속 권고)
legacy-peer-deps=true
